## Lazy pacman commands because lazy
#alias pcup="sudo pacman -Syyu"
#alias pacin="sudo pacman -S "
#alias pacun="sudo pacman -Rsc "
#
## Lazy system commands because lazy
#alias syspend="loginctl suspend"

# Lazy shorthands because lazy
alias e="nvim $1"
alias nvimrc="nvim ~/.config/nvim/"
alias zshrc="nvim ~/.config/zsh-config/" 
alias ls="exa --icons"
alias ll="exa -lah --icons"

# Lazy dir switching because lazy
alias cde="cd /Storage/BigData/$1"
alias cdd="cd /Storage/SmallData/$1"

#
#
# Aliases based on current work
#
#

